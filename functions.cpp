#include "functions.hpp"

// template<class keyType>
// string* Table<keyType>::get(const keyType &key) {
//     auto it = data.find(key);
//     if (it == data.end()) {
//         return nullptr;
//     }
//     return &(it->second);
// }